 <div class="row special_offers">
    {% for specialoffer in specialoffers %}
        <div class="col-md-4 mb-4">
            <div>
                <h5>{{ specialoffer.name }}</h5>
                <div class="media">
                    <img class="mr-3" src="{{ specialoffer.imglink }}" alt="">
                    <p>{{ specialoffer.description }}</p>
                </div>
                <div class="text-center">
                    {% if specialoffer.nolimittimeoffer != 1 %}
                        <p>Période du : {{ specialoffer.startdate }} au {{ specialoffer.finishdate }}</p>
                    {% endif %}
                </div>
            </div>
        </div>
    {% endfor %}
 </div>